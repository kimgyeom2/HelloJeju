<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="vm"
            type="com.gy25m.presentation.ui.login.viewmodel.LoginViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.login.LoginActivity">

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btn_naver_login"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/login_naver"
            android:textSize="16sp"
            android:textColor="@color/white"
            android:padding="6dp"
            android:background="@drawable/bg_login"
            android:backgroundTint="@color/login_naver"
            android:drawableStart="@drawable/icon_naver"
            android:layout_marginHorizontal="20dp"
            android:layout_marginTop="6dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"/>


        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btn_kakao_login"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/login_kakao"
            android:textSize="16sp"
            android:padding="6dp"
            android:background="@drawable/bg_login"
            android:backgroundTint="@color/login_kakao"
            android:drawableStart="@drawable/icon_kakao"
            android:layout_marginHorizontal="20dp"
            android:layout_marginTop="6dp"
            app:layout_constraintTop_toBottomOf="@id/btn_naver_login"
            app:layout_constraintStart_toStartOf="parent"/>

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btn_google_login"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/login_google"
            android:textSize="16sp"
            android:padding="6dp"
            android:background="@drawable/bg_login"
            android:backgroundTint="@color/login_google"
            android:drawableStart="@drawable/icon_google"
            android:layout_marginHorizontal="20dp"
            android:layout_marginTop="6dp"
            app:layout_constraintTop_toBottomOf="@id/btn_kakao_login"
            app:layout_constraintStart_toStartOf="parent"/>




    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>